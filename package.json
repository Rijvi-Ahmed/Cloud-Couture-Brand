{
  "name": "cloud-couture-brand-e2e",
  "version": "1.0.0",
  "description": "Playwright E2E tests for Cloud Couture",
  "scripts": {
    "test:product-creation:debug": "playwright test tests/productCreation.spec.ts --headed --project=chromium",
    "test:save-auth": "playwright test tests/saveAuthState.spec.ts --project=save-auth --headed --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^25.4.0",
    "dotenv": "^16.4.5"
  }
}
